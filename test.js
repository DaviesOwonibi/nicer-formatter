let x = 1;
let y = 1;

function add(x, y) {
  return x + y;
};

for ( ; i > 10; ) {
  let x = 1;
};


for (let x = 1; x > 2; x + 1) {
  return x
};

add(x, y);
console.log(y);
const Person =  {
  name: "Gojo",
  age: 21
};
